'use strict'

// object literals/store locations

var seattle = {
  minCust: 23, 
  maxCust: 65, 
  avgCookiePurch: 6.3,
  Math.floor(Math.random());

};

var tokyo = {
  minCust: 3,
  maxCust: 24,
  avgCookiePurch: 1.2,
  Math.floor(Math.random());
};

var dubai = {
  minCust: 11,
  maxCust: 38,
  avgCookiePurch:3.7,
  Math.floor(Math.random());
};

var paris = {
  minCust: 20,
  maxCust: 38,
  avgCookiePurch: 2.3,
  Math.floor(Math.random());
};

var Lima = {
  minCust: 2,
  maxCust: 16,
  avgCookiePurch: 4.6,
  Math.floor(Math.random());
};